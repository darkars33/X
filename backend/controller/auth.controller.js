
const signUp =  (req, res) =>{
          res.json({
                    message: 'Hello from server'
          })
}

const logIn =  (req, res) =>{
          res.json({
                    message: 'Hello from server'
          })
}

const logOut =  (req, res) =>{
          res.json({
                    message: 'Hello from server'
          })
}

module.exports = {
          signUp,
          logIn,
          logOut
}